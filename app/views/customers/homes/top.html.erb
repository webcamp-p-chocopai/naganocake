<div class='container px-5 px-sm-0'>
  <div class='row mb-5 mt-5'>
    <!-- メインビジュアル -->
    <div class='col-md-6 offset-md-3 mt-5 h5 text-center'>
      <div>
        ようこそ、ながのCAKEへ！<br>
        このサイトでは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。
      </div>
    </div>
  </div>
    <div class='row'>
      <div class='col-md-12 offset-2 h3'>新着商品</div>
    </div>

    <div class='row'>
      <div class='col-md-2'>
      <%= render 'customers/items/genre_sidebar', genres: @genres %>
    </div>
      <% @item.each do |item| %>
      <div class='row float-right'>
        <table class='table table-borderless ml-4'>
          <tr><td>
            <%= link_to item_path(item.id) do %>
            <%= attachment_image_tag item, :item_image, format: 'jpg' , fallback: "no_image_cake.jpg", size: "200x180" %>
            <% end %>
          </td></tr>
          <tr><td><%= item.item_name %></td></tr>
	        <tr><td>¥<%= (item.non_taxed_price*1.1).floor %></td></tr>
        </table>
      </div>
    <% end %>
    </div>
  </div>
</div