<div class='container'>
  <div class='row'>
    <div class='col-md-10'>
      <h4>注文履歴一覧</4>
      <table class='table'>
        <thead>
          <tr>
            <td>注文日</td>
            <td>配送先</td>
            <td>注文商品</td>
            <td>支払金額</td>
            <td>ステータス</td>
            <td>注文詳細</td>
          </tr>
        <tbody>
          <% @orders.each do |order| %>
            <tr>
              <td><%= order.created_at %></td>
              <td><%= order.dear_address %></td>
               <!-- 注文商品紐づいてたっけ？ -->
              <td><%= order.order_items.item_name %></td>
               <!-- 商品の合計金額と送料を足した額を表示、やり方は検索 -->
              <td><%= order.taxed_billing_price.add_tax_price.to_s(:delimited) %> 円 %></td>
              <td><%= order.orderd_status %></td>
              <td><%= link_to 表示する, class: "btn btn-sm btn-success order_path_#{@order.id}"%></td>
            </tr>
          <% end %>
        </tbody>
        </thead>
      </table>
    </div>  
  </div>  
</div>